// Call the function to fetch data and render HTML when the DOM is loaded

import fetchDataAndRenderHTML from "./utils/topvasScript"
document.addEventListener('DOMContentLoaded', function() {
    fetchDataAndRenderHTML();
});